---
title: "Issues"
description: "Generate issues from controls, classify them, and read issues and classifications."
icon: "triangle-exclamation"
---

Related process narrative: [Stage 04 · Issues](/flow/04-issues).

<Warning>
`POST /issues/from-controls/{orgId}` and `POST /issues/classify` have been
**removed**. Both now run automatically as part of `POST /ingest/{clientOrgId}`
— see [Pipeline API](/api-reference/pipeline). You can still start
`issues_from_controls` / `classify_issues` via the generic, non-org-scoped
`POST /jobs` (see [Jobs](/api-reference/jobs)) for debugging.
</Warning>

## List issues

```http
GET /issues?client_org_id={orgId}&include_classification=true&limit=500
```

**Auth:** Bearer · **Returns:** bare JSON array

### Query parameters

| Param | Notes |
| --- | --- |
| `client_org_id` | Scope to the organisation |
| `include_classification` | Embed each issue's classification when `true` |
| `limit` / `offset` | Paging |

### Response `200`

```json
[
  {
    "id": "uuid",
    "title": "Quarterly access review gaps",
    "body": "…",
    "client_org_id": "uuid",
    "issue_scope": "internal",
    "origin": "from_controls",
    "control_ids": ["uuid"]
  }
]
```

---

## Get issue by id

```http
GET /issues/{issueId}
```

**Auth:** Bearer

Single issue. `404 not_found` if missing.

---

## Get issue classification

```http
GET /issues/{issueId}/classification
```

**Auth:** Bearer

### Response `200`

```json
{
  "issue_id": "uuid",
  "classification": { "labels": [], "pestel": {} },
  "model_version": "…",
  "created_at": "…"
}
```

`404 not_found` if the issue has no classification yet.

---

## Issue stats for org

```http
GET /issues/stats/{orgId}
```

**Auth:** Bearer

### Response `200`

```json
{ "status": "success", "data": { "client_org_id": "uuid", "issues": 24, "documents": 3 } }
```
